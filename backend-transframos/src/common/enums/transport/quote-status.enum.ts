export enum QuoteStatus {
  DRAFT = 'draft',
  COLLECTING_DATA = 'collecting_data',
  VALIDATING = 'validating',
  READY = 'ready',
  SENT = 'sent',
  ACCEPTED = 'accepted',
  REJECTED = 'rejected',
  EXPIRED = 'expired',
  CANCELLED = 'cancelled',
}
